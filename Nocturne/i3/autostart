#!/bin/bash

exec --no-startup-id sleep 1

# Background 
exec --no-startup-id feh --bg-fill ~/Pictures/Background/city.png

# Opacity Settings
exec_always --no-startup-id picom -b

# Polybar
exec --no-startup-id ~/.config/polybar/launch.sh
